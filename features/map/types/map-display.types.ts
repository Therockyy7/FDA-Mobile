// features/map/types/map-display.types.ts
// Types liên quan đến chế độ hiển thị bản đồ

/** Loại bản đồ nền: standard / satellite / hybrid */
export type MapType = "standard" | "satellite" | "hybrid";

/** Chế độ xem bản đồ: zones (vùng theo dõi) / routes (tuyến đường) */
export type ViewMode = "zones" | "routes";
